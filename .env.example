# Base de datos (fichas de subscriptores). SQLite para dev, PostgreSQL para producción.
# Copiá este archivo a .env y ejecutá: npm run db:push (crea las tablas en dev.db).
# SQLite (desarrollo local):
DATABASE_URL="file:./dev.db"
# PostgreSQL (producción, ej. Vercel/GCP):
# DATABASE_URL="postgresql://user:pass@host:5432/ptil?schema=public"

# Tipificación de oficios con IA (OpenAI). Sin esta variable se usa respuesta mock.
# OPENAI_API_KEY=sk-...

# Asistente Maestro (dashboard): respuestas con ChatGPT. Opcional: Claude (Anthropic).
# ANTHROPIC_API_KEY=sk-ant-...

# Clave para activar el modo demo en producción. Si no se define, cualquiera puede activar "Modo demo (master key)".
# En Vercel: Project → Settings → Environment Variables → PTIL_MASTER_KEY = tu_clave_secreta
# PTIL_MASTER_KEY=tu_clave_secreta
