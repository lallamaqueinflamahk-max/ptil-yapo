# ========== OBLIGATORIO EN VERCEL ==========
# Base de datos PostgreSQL. Sin esto la inscripción falla en producción.
# Guía completa: docs/NEON-SETUP.md (Neon gratis en 2 minutos).
# Ejemplo rápido: .env.neon.example
DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

# Selfies: subida directa a Vercel Blob (evita error 413).
# Vercel: Project → Storage → Create → Blob → enlazar al proyecto (agrega BLOB_READ_WRITE_TOKEN).
# Local: vercel env pull
# BLOB_READ_WRITE_TOKEN=vercel_blob_rw_...

# ========== OPCIONAL ==========
# Tipificación de oficios con IA (OpenAI). Sin esto se usa respuesta mock.
# OPENAI_API_KEY=sk-...

# Asistente Maestro (dashboard). Opcional: Claude (Anthropic).
# ANTHROPIC_API_KEY=sk-ant-...

# Modo demo en producción.
# Vercel: Settings → Environment Variables → PTIL_MASTER_KEY = tu_clave_secreta
# PTIL_MASTER_KEY=tu_clave_secreta
