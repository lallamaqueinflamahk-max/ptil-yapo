# Copiá este archivo a .env y reemplazá DATABASE_URL con la URL que te da Neon.
# Cómo obtenerla: https://neon.tech → Create project → Connection string.
# Formato: postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require

DATABASE_URL="postgresql://usuario:password@ep-xxxx.region.aws.neon.tech/neondb?sslmode=require"

# Opcional: selfies en Vercel (evita 413). En Vercel: Storage → Blob → enlazar proyecto.
# BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxx

# Opcional: modo demo en producción.
# PTIL_MASTER_KEY=tu_clave_secreta
